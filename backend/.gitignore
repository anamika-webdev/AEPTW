# Ignore uploaded files but keep directory structure
uploads/*
!uploads/.gitkeep
!uploads/*/
!uploads/*/.gitkeep

# But ignore actual uploaded files in subdirectories
uploads/**/*
!uploads/**/.gitkeep